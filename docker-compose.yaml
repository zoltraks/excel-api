services:
  excel-api:
    build:
      context: ./${IMAGE:-excel-api-node}
    ports:
      - "8443:8443"
    volumes:
      - ./work/data/workbooks:/data/workbooks
      - ./work/data/locks:/data/locks
      - ./work/config:/etc/excel-api:ro
    environment:
      - CONFIG_PATH=/etc/excel-api/config.yaml
      - ACCESS_PATH=/etc/excel-api/access.yaml
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8443/health"]
      interval: 5s
      retries: 3
